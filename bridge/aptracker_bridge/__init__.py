"""ApTracker bridge — a small always-on service that watches Archipelago rooms
and forwards item events to Supabase + ntfy so the mobile app can notify you
even while it's closed."""

__version__ = "0.1.0"
